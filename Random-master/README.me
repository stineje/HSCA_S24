These are some C files I wrote to help generate random input streams
that can be used for integrating into a testbench.  A sample script is
given to generate several testvectors for different sizes.

These C files are only meant as suggestions and might need tweaking
based on individual use.
